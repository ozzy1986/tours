MODEL_ID=intfloat/multilingual-e5-small
EMBEDDING_DIM=384
HOST=0.0.0.0
PORT=8001
# true = offline deterministic hash embedder (recommended for local dev).
# false = real HuggingFace model (needs huggingface.co).
USE_STUB=true
# Optional; when set, POST /embed requires X-Api-Key header (apps/api EMBEDDINGS_API_KEY)
EMBEDDINGS_API_KEY=
